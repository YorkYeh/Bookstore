<div class="justify-between w-full shadow navbar">
  <div>
    <%= link_to 'Bookstore', root_path, class:'text-xl normal-case btn btn-ghost my-3'%>
  </div>
  <div class="mx-10">
    <% if user_signed_in? %>
      <%= link_to '購物車', cart_path ,class: "btn btn-ghost"%>
      <%= link_to '登出', destroy_user_session_path, data: { turbo_method: "delete" }, class: "btn btn-ghost" %>
      Hello,<%= current_user.email.split('@').first.capitalize %>
    <% else %>
      <%= link_to '登入', user_session_path, class: 'btn btn-ghost mx-2' %>
      <%= link_to '註冊', new_user_registration_path, class: 'btn btn-ghost' %>
    <% end %>
  </div>
</div>